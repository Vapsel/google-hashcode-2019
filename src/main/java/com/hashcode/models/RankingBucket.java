package com.hashcode.models;

public class RankingBucket {

    public Integer interestFactor;
    public Photo photo;

    public RankingBucket(Integer interestFactor, Photo photo) {
        this.interestFactor = interestFactor;
        this.photo = photo;
    }
}
